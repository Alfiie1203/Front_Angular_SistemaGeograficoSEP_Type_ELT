import { PeriodNamePipe } from './period-name.pipe';

describe('PeriodNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodNamePipe();
    expect(pipe).toBeTruthy();
  });
});
